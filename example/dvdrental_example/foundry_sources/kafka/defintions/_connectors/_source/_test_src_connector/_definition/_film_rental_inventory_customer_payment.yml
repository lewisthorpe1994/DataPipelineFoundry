config:
  tables:
    - name: public.rental
      columns: [
          'rental_id',
          'rental_date',
          'inventory_id',
          'customer_id',
          'return_date',
          'staff_id',
          'last_update'
      ]
    - name: public.film
      columns: [
          'film_id',
          'title',
          'description',
          'release_year',
          'language_id',
          'rental_duration',
          'rental_date',
          'length',
          'replacement_cost',
          'rating',
          'last_update',
      ]
    - name: public.inventory
      columns: [
          'inventory_id',
          'film_id',
          'store_id',
          'last_update'
      ]
    - name: public.customer
    - name: public.payment

